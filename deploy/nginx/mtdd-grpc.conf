upstream mtdd_grpc_backend {
    server 127.0.0.1:50051;
    keepalive 16;
}

server {
    listen 50051 http2;

    location / {
        grpc_pass grpc://mtdd_grpc_backend;
        grpc_read_timeout 3600s;
        grpc_send_timeout 3600s;
        client_max_body_size 0;
    }
}
